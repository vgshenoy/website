{
  "name": "website",
  "version": "1.0.0",
  "description": "Personal works",
  "author": "shenoy",
  "private": true,
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "generate": "nuxt generate",
    "deploy": "npm run generate && cp now.json dist/ && cd dist/ && now --public && now alias"
  },
  "dependencies": {
    "@nuxtjs/google-analytics": "^2.2.0",
    "bulma": "^0.7.5",
    "cross-env": "^6.0.3",
    "nuxt": "^2.10.1"
  },
  "devDependencies": {
    "eslint-config-prettier": "^6.4.0",
    "eslint-plugin-prettier": "3.1.1",
    "image-trace-loader": "^0.1.0",
    "node-sass": "^4.12.0",
    "nodemon": "^1.19.4",
    "prettier": "1.18.2",
    "sass-loader": "^8.0.0"
  }
}
